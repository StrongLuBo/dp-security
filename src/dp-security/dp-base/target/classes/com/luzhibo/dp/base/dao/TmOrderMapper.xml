<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luzhibo.dp.base.dao.TmOrderMapper">

    <resultMap id="BaseResultMap" type="com.luzhibo.dp.base.entity.TmOrderEntity">
            <id property="orderId" column="order_id" jdbcType="INTEGER"/>
            <result property="orderNo" column="order_no" jdbcType="VARCHAR"/>
            <result property="customerId" column="customer_id" jdbcType="INTEGER"/>
            <result property="customerName" column="customer_name" jdbcType="VARCHAR"/>
            <result property="orderStatus" column="order_status" jdbcType="TINYINT"/>
            <result property="payMoneySum" column="pay_money_sum" jdbcType="DECIMAL"/>
            <result property="remarks" column="remarks" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="createUser" column="create_user" jdbcType="VARCHAR"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
            <result property="updateUser" column="update_user" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        order_id,
        order_no,
        customer_id,
        customer_name,
        order_status,
        pay_money_sum,
        remarks,
        create_time,
        create_user,
        update_time,
        update_user
    </sql>
    <select id="listForPage" resultType="com.luzhibo.dp.base.entity.TmOrderEntity">
        select
        <include refid="Base_Column_List"/>
        from tm_order t
        <where>
            <if test="customerName != null and customerName.trim() != ''">
                AND customer_name LIKE concat('%',#{customerName},'%')
            </if>
            <if test="orderNo != null and orderNo.trim() != ''">
                AND order_no LIKE concat('%',#{orderNo},'%')
            </if>
        </where>
        order by t.create_time desc
    </select>

    <insert id="save">
        insert into tm_order(
               order_no,
               customer_id,
               customer_name,
               order_status,
               pay_money_sum,
               remarks,
               create_time,
               create_user
        ) VALUES(
                 #{orderNo},
                 #{customerId},
                 #{customerName},
                 #{orderStatus},
                 #{payMoneySum},
                 #{remarks},
                 now(),
                 #{createUser}
                        )
    </insert>

    <select id="findById" resultType="com.luzhibo.dp.base.entity.TmOrderEntity">
        select <include refid="Base_Column_List"/>
        from tm_order t where t.order_id = #{id}
    </select>

    <update id="update">
        update tm_order
        <set>
            <if test="orderNo != null">order_no = #{orderNo}, </if>
            <if test="customerId != null"> customer_id = #{customerId}, </if>
            <if test="customerName != null">customer_name = #{customerName}, </if>
            <if test="orderStatus != null">order_status = #{orderStatus}, </if>
            <if test="payMoneySum != null">pay_money_sum = #{payMoneySum}, </if>
            <if test="remarks != null">remarks = #{remarks}, </if>
            update_time = now(),
            <if test="updateUser != null">update_user = #{updateUser}, </if>
        </set>
        WHERE
        order_id = #{orderId}
    </update>

    <delete id="batchRemove">
        delete
        from
        tm_order t
        where t.order_id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
